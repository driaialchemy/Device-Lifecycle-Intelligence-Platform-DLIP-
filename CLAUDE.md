# CLAUDE.md — Agent Policy

> Draft generated by AIAlchemy Repo Governor for human review.
> Paste this file at the repository root. Do not commit until you have read it.

## Project Purpose

Streamlit multi-agent device compliance app (R2v3, GDPR, Sensor) with optional PostgreSQL. Uses `.env` for model and database URLs (`.env.example` is the committed template). Several `.xlsx` audit/demo workbooks are already in git.

**Repository:** `Device-Lifecycle-Intelligence-Platform-DLIP-`
**Risk Level:** HIGH

## Allowed Operations

- Read `README.md`, `app.py`, agent modules, and `db/` SQL — not `.env`.
- Edit Streamlit UI or agent prompts in small diffs after the owner accepts the HIGH walkthrough.

## Forbidden Operations

- Do **not** delete files without explicit user confirmation.
- Do **not** commit or push changes automatically.
- Do **not** read, print, or log the contents of `.env`, secrets, or credential files.
- Do **not** install packages outside a virtual environment without asking.
- Do **not** run destructive commands (`rm -rf`, `git reset --hard`, `DROP TABLE`, etc.).
- Do **not** read `.env`.
- Do not connect to a production database.
- Do not treat tracked `.xlsx` files as disposable; they may contain operational demo data.

## Blocked Paths (never read contents)

- `.env`, `.env.*`, `.env.local`, `.env.production`
- `secrets.*`, `credentials.*`, `*.pem`, `*.key`
- Any file under `secrets/`, `private/`, or `credentials/`

## Environment

- **Language(s):** Python, SQL
- **Build / install:** requirements.txt; Docker Compose / local Postgres optional

## In-scope edit areas

Prefer small edits in existing project folders. Stay out of `.git/`, virtualenvs, and secret files.

## Notes

- Copy `.env.example` to a local `.env` only on the owner's machine.
- Spreadsheets are tracked; do not add new dumps of real device/customer data.

## How to verify

```bash
python -m py_compile app.py
```

Always run the verification command before claiming a task is complete.
